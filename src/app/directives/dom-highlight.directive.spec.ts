import { DomHighlightDirective } from './dom-highlight.directive';

describe('DomHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new DomHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
